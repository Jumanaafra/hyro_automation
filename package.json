{
  "name": "hyro-automation-platform",
  "version": "1.0.0",
  "description": "HYRO Automation - Full-stack AI-powered Agentic Workflow Automation Platform",
  "main": "backend/server/src/server.js",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue.bold,green.bold\" \"npm run dev --prefix backend/server\" \"npm run dev --prefix frontend/client\"",
    "dev:server": "npm run dev --prefix backend/server",
    "dev:client": "npm run dev --prefix frontend/client",
    "build": "npm run build --prefix frontend/client",
    "build:client": "npm run build --prefix frontend/client",
    "test:p9": "node backend/server/tests/testRunnerPhase9.js",
    "test": "node backend/server/tests/testRunnerPhase9.js",
    "install:all": "npm install && npm install --prefix backend/server && npm install --prefix frontend/client"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
